# Python cache
__pycache__/
*.pyc
*.pyo
.Python

# Virtual environment (HUGE — never copy this)
.venv/
venv/
env/

# Secrets (NEVER bake .env into image)
.env

# Git
.git/
.gitignore

# VS Code
.vscode/

# Dead code
old_main.py

# Logs
*.log

# Vector stores / Data
chroma_db/
faiss_index/
*.pdf
*.zip
*.tar.gz

# Langfuse self-hosted stack (not needed in app image)
docker-compose.langfuse.yml
langfuse_postgres_data/
langfuse_clickhouse_data/
langfuse_minio_data/

# Docs / CI
README.md
.env.example
